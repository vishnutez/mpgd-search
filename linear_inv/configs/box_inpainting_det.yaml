conditioning:
  method: mpgd_wo_proj
  params:
    scale: 10 # will be changed in another script

data:
  name: ffhq
  root: ./data/samples

measurement:
  operator:
    name: inpainting
    mask_type: box_deterministic
    mask_len_range: [128, 129]
    resolution: 256
    sigma: 0.05
    top: 150
    left: 100
    height: 64
    width: 64
  
  noise: 
    name: gaussian
    sigma: 0.05